language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  # PyPy versions
  - "pypy"
# command to install dependencies
install:
  - pip3 install -r requirements.txt || pip install -r requirements.txt
# command to run tests
script:
  - python3 -m 'nose' || python -m 'nose'
